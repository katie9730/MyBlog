```
1.布尔值定义：let isDone:boolean=false;
2.数字定义： let decliteral:number=6;
3.字符串定义：let name:string="bob";
4.数组定义：方式一：let list:number[]=[1,2,3];
                     方式二：let list:Array<number>=Array<1,2,3>;
5.元组类型定义：let x:[string,number];
                           x=['hello',10];
6.枚举类型定义(主要是为一组数组赋予一个好听的名字):enum Color {Red,Green,Blue};
                         let c:color=color.Green;
```